# Extract evidence

Every claim must include source id, path, heading/region, literal excerpt, confidence
and one status: supported, ambiguous, conflicting, missing or illegible. Only literal
support earns `supported`.
