# S16 — Plano de testes

- Testes de contrato do parser e help/version.
- Process tests do CLI contra homes/config roots temporários e fixtures versionadas.
- Testes negativos para target/opções incompatíveis.
- Inspeção determinística de `package.json` e `npm pack --dry-run --json`.
- Regressão completa dos quinze testes existentes do bundle.
- Gates do monorepo em Windows.
