# RED test plan — S15

- Assert exact skill/subagent inventories and stable model aliases.
- Assert required frontmatter, role names, tool restrictions and safety text.
- Assert capability discovery, BMAD fallback and quick-dev gate text.
- Assert project/user dry-run produces no writes.
- Assert installs preserve config, MCP and unrelated files.
- Assert force creates byte-identical backups before replacement.
- Assert missing model families fail validation.
- Run existing OpenCode/Codex suites to prevent regression.
